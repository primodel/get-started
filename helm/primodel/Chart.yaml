apiVersion: v2
name: primodel
description: Governed canonical data platform — UI, REST, GraphQL and OpenAPI in a single container, backed by PostgreSQL and NATS JetStream.
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Version of the Primodel application shipped by default (image tag fallback).
appVersion: "latest"
